import { styled } from "styled-components";
import { StyledComponentProps } from "../../../types/styled-component-props";

export const LogoStyled = styled.div<StyledComponentProps>`
    img {
        width: 15rem;
    }
`;
import { FileType } from "./file-data";

export type StackDataType = {
    id: number;
    portfolioTitle: string;
    name: string;
    link: string;
    color: string;
    portfolioId: number;
    image: FileType;
}